//go:build tinygo && (darwin || linux || freebsd) && !baremetal && !wasi && !tinygo.wasm
// +build tinygo
// +build darwin linux freebsd
// +build !baremetal
// +build !wasi
// +build !tinygo.wasm

package mem
